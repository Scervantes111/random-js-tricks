function repeat(operation, num) {
      num = 5;
      if (let i = 0; i < 5; i++){
        console.log('This number should show up 5 times' + num);
        i++;
      } else{
        console.log('the End');
      }
    }

    // Do not remove the line below
    module.exports = repeat
